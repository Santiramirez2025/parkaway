# Database (Neon)
DATABASE_URL="postgresql://user:pass@host/db?sslmode=require"
DIRECT_URL="postgresql://user:pass@host/db?sslmode=require"

# Auth
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="" # genera con: openssl rand -base64 32

# Mercado Pago (sandbox al arranque)
# Sacalas de https://www.mercadopago.com.ar/developers/panel/app
MP_ACCESS_TOKEN="TEST-..."
MP_PUBLIC_KEY="TEST-..."
MP_WEBHOOK_SECRET="" # cualquier string, lo validas vos

# WhatsApp Cloud API (Meta)
WHATSAPP_PHONE_NUMBER_ID=""
WHATSAPP_ACCESS_TOKEN=""
WHATSAPP_VERIFY_TOKEN="" # cualquier string para verificar webhook

# Email (Resend)
# Sacala de https://resend.com/api-keys
RESEND_API_KEY="re_..."
EMAIL_FROM="ParkAway <onboarding@resend.dev>"

# Google Maps
GOOGLE_MAPS_API_KEY=""

# Cron jobs (Vercel)
CRON_SECRET="" # genera con: openssl rand -base64 32

# Public
NEXT_PUBLIC_SITE_URL="http://localhost:3000"
NEXT_PUBLIC_GA_ID=""
NEXT_PUBLIC_MP_PUBLIC_KEY=""
